# `jobsvc.auth`

Authentication + authorisation. Two parallel paths — JWT (for the
Playground) and API key (for programmatic use).

## Passwords

::: jobsvc.auth.passwords
    options:
      members_order: source
      show_root_full_path: false

## JWT

::: jobsvc.auth.jwt
    options:
      members_order: source
      show_root_full_path: false

## API keys

::: jobsvc.auth.api_key
    options:
      members_order: source
      show_root_full_path: false

## FastAPI dependencies

::: jobsvc.auth.deps
    options:
      members_order: source
      show_root_full_path: false
